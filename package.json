{
  "name": "bo-portfolio-blog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bo Tembunkiart's unified portfolio and blog",
  "author": "Bo Tembunkiart <botbunk@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm build:content && vite",
    "build:content": "node scripts/build.js",
    "build:images": "node scripts/process-images.js",
    "build": "pnpm build:images && pnpm build:content && vite build && mkdir -p dist/images && cp -r blog dist/ && cp -r projects dist/ && cp -r css dist/ && cp -r js dist/ && cp -r images/optimized dist/images/optimized",
    "preview": "vite preview",
    "clean": "rm -rf dist blog projects images/optimized",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.0.0",
    "markdown-it-attrs": "^4.1.6",
    "sharp": "^0.34.5",
    "vite": "^5.0.11",
    "vitest": "^4.0.18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Aklaran/aklaran.github.io"
  },
  "dependencies": {
    "nunjucks": "^3.2.4"
  }
}
